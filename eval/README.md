# Eval / comparison

Compare any two match JSON files (e.g. two uploads or hand-edited baselines):

```bash
npm run compare:json -- eval/baseline.json eval/candidate.json
```

Requires `python3` on PATH (script uses the standard library only).
