# API key and secrets (never commit)
.env
.env.*
!.env.example

# Persisted match data (optional: remove from .gitignore to track)
data.json

# Uploaded scoresheets
uploads/

# Last Groq extraction parse (for inspection)
last-extraction.json
gemini-response.json

# Debug: prompt + image for manual UI comparison
debug-prompt.txt
debug-upload.*

# Dependencies
node_modules/

# Python (local tooling / legacy; not used by Node server)
.venv/
__pycache__/
*.pyc
_ocr_test.png

# Cursor IDE (local only; do not commit)
.cursor/
