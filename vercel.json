{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["*.html", "assets/**", "schema.json", "extraction-prompt.txt"]
      }
    }
  ],
  "routes": [{"src": "/(.*)", "dest": "/server.js"}]
}
